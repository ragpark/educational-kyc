# requirements.txt - Railway-tested minimal version

# Core FastAPI (known to work on Railway)
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Templates and forms
jinja2==3.1.2
python-multipart==0.0.6

# HTTP requests (essential for API calls)
aiohttp==3.9.1
requests==2.31.0

# Data validation
pydantic==2.5.0

# Basic logging (no external dependencies)
python-dotenv==1.0.0

# Date utilities (pure Python)
python-dateutil==2.8.2
